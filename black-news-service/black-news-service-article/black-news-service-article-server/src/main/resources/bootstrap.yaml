server:
  port: 51802
spring:
  application:
    name: black-news-article
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
